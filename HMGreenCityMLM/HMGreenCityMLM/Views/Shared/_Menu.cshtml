@model HMGreenCityMLM.Models.Home

<ul class="navigation navigation-main" id="main-menu-navigation" data-menu="menu-navigation">

    <li class="nav-item">
        <a href="/Admin/AdminDashBoard"><i class="feather icon-mail"></i><span class="menu-title" data-i18n="Email">Dashboard</span></a>
    </li>
    @if (Model != null)
    {
        if (Model.lstMenu != null)
        {


            foreach (var item in Model.lstMenu)
            {
                <li class=" nav-item">
                    <a href="#"><i class="feather icon-shopping-cart"></i><span class="menu-title" data-i18n="Ecommerce">@item.MenuName</span></a>
                    @if (item.Url == "#")
                    {
                        <ul class="menu-content">
                            @foreach (var item1 in Model.lstsubmenu)
                            {
                                if (item1.MenuId == item.MenuId)
                                {
                                    <li>
                                        <a href="@item1.Url"><i class="feather icon-circle"></i><span class="menu-item" data-i18n="Shop">@item1.SubMenuName</span></a>
                                    </li>
                                }
                            }



                        </ul>
                    }
                </li>
            }
        }
    }





</ul>
